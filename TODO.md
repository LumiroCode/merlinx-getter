# Priority
## Release package for Andrzej
TBD

# Postponed
## migrate tests to PHPUnit
Tests now are running on makeshift shared unilities and with own reporter sh script. This is not ideal, so we want to migrate them to PHPUnit framework, which is more standard and has better features. It will also ease test coverage and test result checks for AI Agents, as they can rely on PHPUnit's structured output and features.
